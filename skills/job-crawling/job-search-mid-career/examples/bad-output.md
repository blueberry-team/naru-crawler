# Bad Output Example

중도채용 초안에서 특히 피해야 하는 것:

- `experienceLevel` 을 `NEW_GRAD` 로 두는 것
- `position` 에 `GRADUATE_*` enum 을 쓰는 것
- `joinDate` 를 근거 없이 `2027년 4월` 같은 신입 규칙으로 넣는 것
- `targetAudience` 에 기술 스택, 비자, 어학 요건까지 섞는 것
