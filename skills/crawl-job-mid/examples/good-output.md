# Good Output Example

중도채용용 좋은 예시는 아직 작성 전이다.

초안 검수 포인트:

- `experienceLevel = MID_CAREER`
- `position` 은 `GRADUATE_*` 가 아닌 백엔드 enum
- `joinDate` 는 `입사 시기 협의` / `가능한 한 빠르게` 같은 중도채용 표현
- `targetAudience` 는 경력 연차 / 직무 중심
